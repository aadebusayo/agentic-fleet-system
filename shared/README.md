# Shared Contracts

This package contains cross-service contracts:

- A2A message schema
- MCP tool contract schema
- Agent definition and capability manifest schemas
- Policy decision schema
- State/memory interfaces

All service boundaries should validate payloads against these schemas.
